/**
 * Builds a query object using the current document object model (DOM).
 * Must use the browser's global document object {@link https://developer.mozilla.org/en-US/docs/Web/API/Document}
 * to read DOM information.
 *
 * @returns query string, the string should be
 *   should be the URL-encoded version of an address 
 *   (e.g., 6245 Agronomy Road V6T 1Z4 should be represented as 6245%20Agronomy%20Road%20V6T%201Z4)
 */
function buildQuery() {
    let query = '';
    
    // TODO: assign the value of textarea to query, make sure it is URL-encoded

    return query;
};
